import Layout from "../components/Layout";
import SelectionComponent from "@/components/SelectionComponent";

const Selection = () => {
  return (
    <Layout title="CICO" serviceType="cico">
      <SelectionComponent />
    </Layout>
  );
};

export default Selection;
